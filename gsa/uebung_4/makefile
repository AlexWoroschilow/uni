CC=cc
CFLAGS=-c
LDFLAGS=-o
SOURCES=$(wildcard *.c)
OBJECTS=$(SOURCES:.c=.o)
EXECUTABLE=$(SOURCES:.c=.x)

all: $(OBJECTS) $(EXECUTABLE)

$(OBJECTS): $(SOURCES)
	$(CC) $(CFLAGS) $(SOURCES) 

$(EXECUTABLE): $(OBJECTS)
	$(CC) $(OBJECTS) $(LDFLAGS) $(EXECUTABLE)