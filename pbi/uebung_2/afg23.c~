

  #include <stdio.h>
  #include <math.h>

  int main()
  {
    int r;
    int qarea; 
    float carea;
    int qperimeter;
    float cperimeter;
    
    qarea = r * r;
    qperimeter = 4 * r;
    carea = M_PI * r * r;
    cperimeter = M_PI * 2 * r;
    return(0);
  }
    
    
  