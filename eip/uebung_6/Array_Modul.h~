

  /* Arraz_Modul.h: Scannen, invertieren, sortieren und binaer durchsuchen von Arrays. */
  
  extern int scanArray(int arr1[], int length);
  extern int invertArray(int arr1[], int length);
  extern int sortArray(int arr1[], int length);
  extern int halb(int i, int j, int length, int par[], int arr1[], int x);
  extern int binSearchArray(int arr1[], int length);
